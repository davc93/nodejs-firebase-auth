{
  "name": "firebase-auth-express",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {

    "dev": "pnpm --filter './apps/**/*' run dev",
    "build": "pnpm --filter './apps/**/*' run build",

    "start": "pnpm --filter './apps/**/*' run start",
    "lint": "pnpm --filter './apps/**/*' run lint",
    "lint:fix":"pnpm --filter './apps/**/*' run lint:fix",
    "precommit": "pnpm --filter frontend run lint:fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "husky": {
    "hooks": {
      "precommit": "pnpm run precommit"
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^8.0.3"
  }
}
